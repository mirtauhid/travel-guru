import React from 'react';
import Header from '../Header/Header';

const Destination = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Destination;